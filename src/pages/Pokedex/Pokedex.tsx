import React from 'react';
import Heading, {HeaderType} from "../../components/Heading/Heading";

const Pokedex = () => {
  return (
    <div>
      <Heading type={HeaderType.h1}>Pokedex</Heading>
    </div>
  );
};

export default Pokedex;