import React from 'react';
import Heading, { HeaderType } from '../../components/Heading/Heading';

const Legendaries: React.FC = () => {
  return (
    <div>
      <Heading type={HeaderType.h1}>Legendaries</Heading>
    </div>
  );
};

export default Legendaries;
